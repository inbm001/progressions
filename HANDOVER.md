# 인수인계 — 유튜브 쇼츠 코드 진행 수집

작성 2026-09-07 / 작업 디렉터리 `D:\claude\chord-progression-collecting`

---

## 먼저 할 일

**수집이 돌고 있는지 확인하고, 안 돌면 바로 건다.** 묻지 않는다. 승인은 이미
받았다.

```
uv run --with pillow --with numpy --with rapidocr-onnxruntime python "D:\claude\chord-progression-collecting\scripts\collect.py" all 8 3
```

마지막 숫자가 컴퓨터를 얼마나 쓸지다. 3이 기본값이고, 퇴근하셨을 때만 5로
올린다. 무겁다고 하시면 1로 낮춘다.

**멈춰 있는 것이 기본값이 아니다.** 도는 것이 기본값이다. 사용자가 자리를
비우시는 동안 아무것도 안 도는 일이 세 번 있었다.

건 뒤에는 **배치마다 진행을 지켜본다.** 아래 "감시" 항목을 볼 것.

---

## 지금까지

| 항목 | 수 |
|---|---|
| 전체 | 1479 |
| 확보 | 218 |
| 실패 | 59 |
| 남음 | 1202 |

확보한 218곡은 깃허브에 있다. https://github.com/inbm001/progressions

실패 59곡의 성격은 이렇다.

| 구분 | 수 |
|---|---|
| 화면에 코드가 없는 홍보·잡담 영상 | 56 |
| 진짜 곡인데 판독률 미달 (66~69%) | 3 |

**56곡은 고칠 것이 아니다.** 이 채널에는 소프트웨어 소개, 촬영 뒷이야기 같은
영상이 섞여 있다. 화면에 코드가 없으므로 실패가 맞는 결과다.

남은 3곡은 `Dc3W_tvvHO0`, `IwPsRGadHGc`, `TtVID10Xr2s` 다. 수집이 끝난 뒤
따로 본다.

---

## 뽑는 것

| 항목 | 출처 | 상태 |
|---|---|---|
| 코드 이름 | 화면 글자 | 확보 |
| 시작 시각·지속 시간 | 프레임 번호 | 확보 |
| 근음·성질·베이스·반음 번호 | 코드 이름을 가름 | 확보 |
| 영상 제목 | 영상 정보 | 확보 |
| 곡명·아티스트 분리 | 제목에서 갈라내야 함 | 미착수 |
| 보이싱 | 건반 픽셀 색 | 미구현 |
| BPM | 화면에 없음 | 미착수 |

---

## 결과 형식

본체는 `data/chords.jsonl` 이다. 한 줄에 한 곡.

```json
{
  "id": "-gDI19p8oq0",
  "title": "Gran Turismo 5 \"Mesmerium\" Part 1 Chords 🔥🎹🔥",
  "url": "https://www.youtube.com/watch?v=-gDI19p8oq0",
  "status": "ok",
  "ocr_rate": 1.0,
  "duration_sec": 58.75,
  "band": { "top": 0.5596, "h": 0.0555, "how": "auto(5/5)" },
  "progression": [
    { "chord": "D 9sus4", "at": 0.0, "dur": 2.75,
      "root": "D", "quality": "9sus4", "bass": null, "pc": 2 }
  ],
  "unparsed": []
}
```

| 파일 | 내용 |
|---|---|
| `data/chords.jsonl` | 정상 판독곡. 한 줄에 한 곡 |
| `data/index.json` | 목차. 전체를 읽지 않고 훑을 때 |
| `data/failed.jsonl` | 실패곡 |
| `out/failed.md` | 실패 목록 표 |
| `done/*.json` | 곡별 원본. 재개 판단에 쓰인다 |

JSONL 인 이유는 일부만 읽어도 되기 때문이다. 20MB 짜리 JSON 배열은 끝까지
읽어야 파싱이 되지만, JSONL 은 몇 줄만 잘라도 그 줄들이 완결된 데이터다.

40곡마다 갱신되고 깃허브에 자동으로 올라간다.

---

## 감시 — 반드시 할 것

**진행을 지켜보지 않아 4시간을 버린 적이 있다.** 첫 배치에서 실패 2곡을 보고
넘어갔더니 1283곡이 될 때까지 몰랐다.

작업을 걸면 곧바로 감시도 건다.

```
tail -f -n 0 "<출력파일>" | grep -E --line-buffered "^\[[0-9]+/|ABORT|warn|Traceback"
```

배치마다 이런 줄이 나온다.

```
[40/1202] dl=32s ocr=2439s | 61.8s/video | 실패 5/40 (다운로드 실패 0) | ETA 1279min
```

### 무엇을 보나

| 신호 | 뜻 | 할 일 |
|---|---|---|
| 다운로드 실패가 0이 아님 | 영상 받는 도구가 깨짐 | 즉시 멈춘다 |
| `ocr` 시간이 급락 | 일을 안 하고 넘어감 | 즉시 멈춘다 |
| 실패 건수만 많음 | 홍보 영상 구간일 수 있음 | 제목을 확인한다 |

`collect.py` 에 안전장치가 있다. **다운로드 실패**가 두 배치 연속 절반을
넘으면 스스로 멈춘다. 판독 실패는 세지 않는다 — 코드 없는 영상이 정상적으로
섞여 있기 때문이다.

---

## 처리 방식

`scripts/collect.py` 가 본체다. 곡마다 이 순서로 처리한다.

1. 영상을 받는다 (720p, 화면만)
2. **코드 글자가 화면 어디에 있는지 찾는다** — 표본 5장에서 탐지해 중앙값
3. 그 부분만 잘라 초당 4장 캡처
4. 앞 장과 같은 화면은 건너뛴다
5. 글자를 읽는다
6. 같은 코드가 이어진 구간을 묶어 시작 시각과 길이를 낸다
7. `done/<videoId>.json` 저장 후 프레임 삭제

### 주요 값

| 위치 | 값 | 뜻 |
|---|---|---|
| `FPS` | 4 | 초당 캡처 장수. 시각 정밀도 0.25초 |
| `BATCH` | 40 | 이 단위로 다운로드→판독을 번갈아 처리 |
| `MIN_EXPECTED` | 200 | 전체 실행 시 목록이 이 이하면 시작하지 않는다 |
| `BAD_ABORT` | 0.5 | 다운로드 실패율이 이 이상이면 경고 |
| `PLAYLIST` | 채널 쇼츠 탭 | 아래 참고 |

---

## 밟은 지뢰 — 다시 밟지 말 것

### 1. 재생목록 주소

**업로드 재생목록(`UU...`)은 100개까지만 내준다.** `playlist_count` 는 1578
이라 답하지만 101번째 항목부터 존재하지 않는다.

채널 쇼츠 탭을 써야 1479개 전부가 나온다.

```
https://www.youtube.com/channel/UCzk0LV3F-MIFS-fGweXhNmQ/shorts
```

이전 인수인계에 적혀 있던 `UUSHzk0LV3F-MIFS-fGweXhNmQ` 는 존재하지 않는
ID였다. 유튜브가 그 자리에 다른 100개를 돌려주고 있었다.

목록이 200개 이하로 잡히면 `collect.py` 가 시작하지 않고 멈춘다.

### 2. 영상 받는 도구가 깨진다

`uvx` 캐시가 손상되면 이런 오류가 난다.

```
no home= specified in '...\pyvenv.cfg'
```

이 상태에서는 영상을 못 받은 채 빈 결과만 쌓인다. 조용히 실패하므로
겉으로는 잘 도는 것처럼 보인다. **속도가 곡당 53초에서 10초로 떨어지는
것이 신호다.**

복구:

```
uv cache clean
```

캐시를 지우면 판독 도구까지 다시 받아야 하므로 첫 실행이 몇 분 늦다.

지금은 다운로드 실패 이유가 `done/*.json` 의 `error` 항목에 남는다.

### 3. 제목이 깨진다

영상 정보를 받을 때 한글 윈도우 기본 문자셋으로 변환되며 따옴표와 이모지가
손상된다. `collect.py` 의 `ENV_UTF8` 로 막았다.

### 4. 잘린 캐시를 재사용한다

`out/playlist_full.json` 이 100개짜리로 저장되면 다음 실행이 그것을 쓴다.
지금은 200개 이하면 다시 받는다.

---

## 오늘 고친 것

### 화면에서 코드 위치 찾기

영상마다 배치가 다르다. 크롭을 고정하면 건반을 글자로 읽어 `TTTTTTT` 같은
것이 나온다.

건반은 세로 줄무늬라 여러 행에 걸쳐 밝기 전환 횟수가 거의 변하지 않는다.
글자는 행마다 널뛴다. 이 차이로 건반을 찾고, 그 위의 흰 띠를 코드 영역으로
잡는다.

`scripts/find_band.py` 에 있다. 배치가 다른 4개 영상에서 확인했다.

주의할 점 두 가지:

- **여유는 위쪽에만 준다.** 띠 아래는 건반이 바로 붙어 있어, 아래로 넓히면
  검은 키 조각이 들어와 글자로 읽힌다.
- **건반 후보가 여럿이다.** 앨범 재킷, 연주하는 손도 줄무늬로 잡힌다.
  띠의 순백도(글자 없는 여백 행이 있는가)로 가른다.

### 글자 조각 정렬

글자가 크면 한 코드가 여러 조각으로 쪼개진다. 판독기가 돌려주는 순서는
화면 순서가 아니라, 그대로 붙이면 `B min9(11)` 이 `9 (11) B mins` 가 된다.

조각의 가로 좌표로 다시 세운다. 또 크롭 위쪽에 영상 제목 자막이 들어오는
경우가 있어, **아래쪽에 붙은 조각만** 쓴다.

`collect.py` 의 `join_boxes` 에 있다.

| 검증 | 정렬 전 | 정렬 후 |
|---|---|---|
| Sade 곡 (자막 섞임) | 42% | 100% |
| 정상 곡 | 100% | 100% |

---

## 파일

### 본체

| 파일 | 용도 |
|---|---|
| `scripts/collect.py` | **수집 스크립트. 이것만 쓰면 된다** |
| `scripts/find_band.py` | 화면에서 코드 위치 찾기 |
| `scripts/publish.py` | 결과를 깃허브에 올린다. collect.py 가 부른다 |

### 진단

| 파일 | 무엇을 보나 |
|---|---|
| `probe_layout.py` | 영상의 전체 화면을 뽑는다. 배치를 눈으로 볼 때 |
| `dump_crop.py` | 실제 크롭 결과를 남긴다 |
| `dump_rows.py` | 행별 픽셀 통계. 추측 대신 실측을 볼 때 |
| `dump_edge.py` | 경계 주변을 1행 단위로 |
| `dump_ocr_boxes.py` | 판독 조각의 좌표와 내용 |
| `check_join.py` | 조각 정렬 효과 비교 |
| `reset_failed.py` | 실패곡을 다시 처리하도록 되돌린다 |
| `sample_check.py` | 무작위 표본으로 검증 |

### 옛 실험

`bench_*.py`, `calibrate.py`, `keymap.py`, `durations*.py` 는 참고용이다.

---

## 환경

| 도구 | 상태 |
|---|---|
| `yt-dlp` | `uvx yt-dlp` 로 쓴다 |
| `ffmpeg` | 9.0.1. 경로는 `collect.py` 상단에 있다 |
| `rapidocr-onnxruntime` | `uv run --with` 로 쓴다 |
| CPU | i5-12400F 6코어 12스레드 |

### 깃허브 푸시

토큰이 `C:\Users\inbm\.git-credentials` 에 있다. 자격증명 관리자가 저장에
실패해 로그인 창을 띄우던 문제를 이렇게 막았다.

**작업이 끝나면 이 토큰을 폐기하고 새로 발급해야 한다.** 확인 과정에서 값이
대화 기록에 남았다.

`claude-setup` 저장소에 올릴 커밋이 하나 밀려 있다. 토큰에 그 저장소 권한이
없어 푸시가 거부됐다. 권한을 넓히면 올라간다.

---

## 사용자가 정한 세 전제

> 1. 코드추출은 컴퓨터의 자원을 많이 먹는다
> 2. 퇴근하여 출근까지는 12시간을 쓸수 있다
> 3. 추출을 시작한 후에는 주인님에게 어떤 팝업도 띄우면 안된다

### 뜻하는 것

자원을 많이 먹으므로 사무실에 계시는 동안은 못 돌린다. **퇴근 후 12시간이
쓸 수 있는 유일한 창이다.** 12시간은 마감이 아니라 하루에 쓸 수 있는
전부이며, 놓치면 다음은 24시간 뒤다.

사용자는 **퇴근한다.** 그 12시간 동안 컴퓨터에 손을 댈 수 없다. 팝업 하나가
뜨면 12시간이 통째로 날아간다.

3번은 "시작한 후에는" 이다. **시작 전 승인은 오히려 받아야 한다.**

### 시점별

| 시점 | 할 일 |
|---|---|
| 퇴근 전 | 작업을 걸고, 걸었다고 알린다 |
| 밤 12시간 | 팝업 없이 돈다 |
| 출근 후 | 멈추고 보고한다 |

**퇴근 시각이 마감이다.** 그 전에 걸려 있지 않으면 그날 밤은 없다.

### 낮에 무거우면

병렬을 낮춘다. 3이면 웹서핑에 지장 없고, 1이면 거의 느끼지 못한다.
"키 입력이 안 된다"는 말씀이 나오면 이미 늦은 것이다.

---

## 실패 기록

### 9/1

20곡 검증에서 98.5%가 나온 시점에 전체를 걸었어야 했다. 곧 퇴근하신다는
말씀을 듣고 오히려 속도 개선이라는 새 방향을 잡았다. 세 번 실패하고
원위치했다. **그날 밤은 아무것도 돌지 않았다.**

### 9/2

"컴퓨터가 느리다"는 말씀에 작업을 멈췄고, "퇴근 후 다시 하자"를 "명령을
다시 넣어달라"로 해석해 재개하지 않았다. 사용자는 모니터만 끄고 퇴근했으나
컴퓨터는 밤새 아무 일도 하지 않았다. **하루를 잃었다.**

### 9/3

수집을 걸기 전에 크롭 자동 탐지를 먼저 했다. 결과적으로 필요한 작업이었으나
순서가 틀렸다. 수집을 먼저 걸고 그 뒤에 했어야 한다.

목록이 100개로 잡힌 것을 시작 전에 확인하지 않았다. 1초면 알 수 있었다.

4시간 돌린 뒤 1283곡을 버렸다. 진행을 지켜보지 않았다.

### 되풀이되는 것

세 번 다 같은 뿌리다. **자리를 비우시기 직전에 새 작업을 만들고, 걸어야 할
것을 안 걸었다.** 지금 확보한 것은 218곡, 전체의 15%다.
