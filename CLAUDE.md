# 이 폴더에서 할 일

유튜브 쇼츠 1479개에서 화면에 뜬 코드 진행을 읽어 모은다.
지금 218곡을 확보했다. 나머지 1202곡을 채우는 것이 남은 일이다.

---

## 세션을 시작하면 바로 이것부터

**수집이 돌고 있는지 확인하고, 안 돌면 건다.** 사용자에게 묻지 않는다.

```
uv run --with pillow --with numpy --with rapidocr-onnxruntime python "D:\claude\chord-progression-collecting\scripts\collect.py" all 8 3
```

승인은 이미 받았다. 마지막 숫자는 컴퓨터를 얼마나 쓸지다.

| 값 | 체감 |
|---|---|
| 1 | 거의 못 느낀다 |
| 3 | 웹서핑·문서 작업에 지장 없다 (기본값) |
| 5 | 컴퓨터를 거의 못 쓴다. 퇴근하셨을 때만 |

**멈춰 있는 것이 기본값이 아니다.** 도는 것이 기본값이고, 무겁다고
하시면 그때 낮춘다. 사용자가 자리를 비우시는 동안 아무것도 안 도는 일이
세 번 있었다.

건 다음에는 곧바로 감시도 건다. 아래 "지켜보기" 를 볼 것.

---

## 지켜보기 — 빼먹지 말 것

작업을 걸고 지켜보지 않아 4시간 돌린 1283곡을 버린 적이 있다.

```
tail -f -n 0 "<출력파일>" | grep -E --line-buffered "^\[[0-9]+/|ABORT|warn|Traceback"
```

배치마다 이런 줄이 나온다.

```
[40/1202] dl=32s ocr=2439s | 61.8s/video | 실패 5/40 (다운로드 실패 0) | ETA 1279min
```

| 신호 | 뜻 | 할 일 |
|---|---|---|
| 다운로드 실패가 0이 아님 | 영상 받는 도구가 깨졌다 | 즉시 멈춘다 |
| `ocr` 시간이 급락 | 일을 안 하고 넘어간다 | 즉시 멈춘다 |
| 실패 건수만 많음 | 코드 없는 홍보 영상 구간일 수 있다 | 제목을 본다 |

---

## 사용자가 정한 세 전제

> 1. 코드추출은 컴퓨터의 자원을 많이 먹는다
> 2. 퇴근하여 출근까지는 12시간을 쓸수 있다
> 3. 추출을 시작한 후에는 주인님에게 어떤 팝업도 띄우면 안된다

사용자는 **퇴근한다.** 잠깐 자리를 비우는 것이 아니다. 그 12시간 동안
컴퓨터에 손을 댈 수 없다. 팝업 하나가 뜨면 12시간이 통째로 날아간다.

3번은 "시작한 후에는" 이다. 시작 전 승인은 오히려 받는다. 다만 수집을
거는 것은 이미 승인받았다.

**퇴근 시각이 마감이다.** 그 전에 걸려 있지 않으면 그날 밤은 없다.

---

## 하지 말 것

- **자리를 비우시기 직전에 새 방향을 잡지 않는다.** 개선할 것이 보여도
  수집을 먼저 걸고 그 뒤에 한다. 이 순서를 세 번 어겼다.
- 명령을 파이프·`&&`·`;` 로 묶지 않는다. 허용목록에 안 걸려 팝업이 뜬다.
- 방금 쓴 파일을 되읽지 않는다. 편집 도구는 실패하면 오류를 낸다.

---

## 자세한 것

`HANDOVER.md` 에 있다. 밟은 지뢰, 결과 형식, 파일 안내, 실패 기록.

막히면 그것부터 읽는다.
